# Web/API + production serving
Flask>=3.0.0
Werkzeug>=3.0.0
gunicorn>=22.0.0

# Conversion core
Pillow>=9.0.0
PyPDF2>=3.0.0
pydub>=0.25.1

# Optional/extended format support
python-docx>=0.8.11
pandas>=1.3.0
openpyxl>=3.0.0
reportlab>=3.6.0
xlrd>=2.0.0
xlwt>=1.3.0
markdown>=3.3.0
weasyprint>=54.0
odfpy>=1.4.1
python-magic>=0.4.24
chardet>=4.0.0
imageio>=2.9.0
opencv-python>=4.5.0
pyheif>=0.7.1; python_version < "3.14"
rawpy>=0.17.0

# Notes:
# - pyheif builds native extension and currently fails commonly on Python 3.14.
# - For Python 3.14, install system libheif development headers and try manual install if needed:
#   sudo dnf install libheif libheif-devel libffi-devel gcc python3-devel
#   pip install pyheif
